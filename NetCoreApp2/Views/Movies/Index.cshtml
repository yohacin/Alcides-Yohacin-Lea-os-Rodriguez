@model IEnumerable<Entities.Movie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.year)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.year)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_Movie">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_Movie">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_Movie">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
